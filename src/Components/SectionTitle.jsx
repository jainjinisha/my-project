import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h1 className="text-bold text-white text-5xl text-center py-[70px]">
      {title}
    </h1>
  );
};

export default SectionTitle;
